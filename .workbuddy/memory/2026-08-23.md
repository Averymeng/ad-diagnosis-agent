
## 2026-08-23 作品集截图
- 用户问为何报告「异常与原因」「案例参考」常为空：解释为正常周无异常故空、案例库仅21条匹配不到故空；不是 bug，是真实业务结果。
- 用户要两张尺寸一致、美化后的作品集截图。用 agent-browser 从本机动态服务 (localhost:8000) 截取：
  1. 主界面（客户详情卡片网格） → 01_main_final.png
  2. 周度复盘报告（远山出境游，全量 LLM 生成） → 02_report_final.png
- 用 Pillow 加了浏览器窗口 chrome、圆角、投影、浅蓝背景，统一尺寸 1440×900。
- 文件保存在 求职材料/screenshots/（gitignored）。

## 2026-08-23 续：案例库扩建 + 截图
- 案例库挖矿脚本 `诊断台/eval/mine_cases.py` 重写：报告 JSON 的 chapter 5_异常与原因 实际结构是 {top3_detail:[...], watchlist:[...]}，且部分报告 top3_detail 被逐字拆成单字符列表（需拼接）。修复抽取逻辑后挖出 35 条候选，直接写入 diag_case（21→56 条）。
- 重跑 `eval/run_eval.py`：**126/126 仍全绿**，扩建未破坏诊断引擎保护网。
- 截图：用 agent-browser 从 localhost:8000 截 customer 全页 + 报告全页（customers_full.png / report_full.png），真实完整内容、统一视口宽度，无假边框。

## 部署更新（10:45）
- 后台重生成 51 客户报告（r2G9Dz）完成：全部 OK，0 失败；案例库 56 条已写入各报告第 6 章。
- 重跑 static_export.py 导出 web/snapshot.json（51 客户/报告）。
- 重新 CloudStudio 部署 诊断台/web/ → https://349a08f059e14b86937b3346fa9a6d15.app.workbuddy.link（静态只读大盘，含新案例）。
- 提醒：静态部署为快照，后续改数据需重导+重部署。

## 销售工作台扩建（14:00-14:30）
- 新增 web/workbench.js：纯前端只读分析层，基于 snapshot 计算，不碰诊断引擎（满足硬约束）。
- 功能：健康分(100分公式，消耗动能+指标惩罚+状态修正+异常惩罚)/预警/分群(健康29 关注13 预警9)、打法推荐(规则化+同赛道案例库匹配)、覆盖度自检矩阵。
- static_export.py 加 cases 只读导出(56条)；index.html/app.js/style.css 挂载导航与视图。
- 部署：CloudStudio 重部署 + gh-pages 推送(be263e8)；代码 commit 618696d/714a42a 至 main。
- 遗留：diag_case 中 AI培训 赛道 23 条 action_taken 为占位符，导致该赛道打法推荐无案例匹配（方案2 数据清理待办）。

## 客户经营模块改名 + 去冗余重构（15:00-15:10）
- 用户纠正命名层级：产品名=销售工作台，新增子模块改名「客户经营」（原错叫销售工作台）。
- 页面去冗余：删 51 名全量健康分表→分群概览3卡(点开展开)；风险预警只列tier=预警(约9)；覆盖度改为按赛道汇总+仅列缺口。

## 广告销售工作台 UI 调整 + 方案2 数据清理（15:00-15:20）
- 侧边栏顶部「诊断台」改为「广告销售工作台」；去掉 MAIN/销售 分组标题；导航顺序：客户经营、当日监控、客户详情、手动录入。
- 客户经营页面删除「销售工作台 · 客户经营看板（只读分析）」副标题，删除「风险预警」独立列表。
- 完成方案2：将 diag_case 中 AI培训 赛道 23 条占位符案例全部标记 referenceable=0（10 条 action_taken=None，13 条为「待补：来自策略章节」），避免污染打法推荐与覆盖度统计。
- 重跑 static_export.py 更新 snapshot.json；CloudStudio + gh-pages 重新部署；main 提交 1935dcf/a68dd9d。

## 客户经营 UI 二次优化（15:30-15:45）
- 覆盖度自检：去掉占空间的逐赛道大表（约 13 行）与说明段，压缩为「诊断 x/y · 案例覆盖赛道 a/b · 缺口 n」一行摘要 + 缺口客户小标签（wb-cov 样式）。
- 同赛道历史打法：从纯色平铺文字改为分点卡片（wb-match）：左侧紫色强调边、案例 id 紫标 + 赛道绿标；异常签名橙色提示框；打法按 。；；\n 分点、五彩色圆点（蓝/绿/紫/黄/红循环）；结果绿色高亮框。
- 新增 splitPoints() 辅助；main 提交 369c043，gh-pages cb4b03e。CloudStudio 部署本次遇到 504 暂未成功（GitHub Pages 已上线）。
- 改文件 web/{index.html,app.js,workbench.js,style.css}+操作日志。main b627ea3；gh-pages bae56f9；CloudStudio 已重部署。
- 方案2(AI培训23条占位符案例清理)按用户约定"一会儿一起行动"，本次未做。
EOF\necho done