# 诊断台 · 操作日志

记录关键运维动作（案例库扩充、报告重生成、部署发布），便于追溯。

---

## 2026-08-23 案例库扩充 + 全量报告重生成 + 重新部署

**操作人**：AIPM 项目协作
**时间**：2026-08-23 10:00–10:50 (GMT+8)

### 背景
用户发现存量客户报告中第 6 章「案例参考」为空。原因：报告生成时缓存了当时的案例库快照，后续新增案例不会自动回填。

### 动作
1. **案例库扩充（脚本挖矿）**
   - 新增脚本 `eval/mine_cases.py`：从存量报告 JSON 抽取异常/策略，按 `diag_case` schema 起草候选并入库。
   - 案例库从 21 条扩充至 **56 条**（零新增 LLM 成本、真实、可批量）。
   - 重跑评测 `eval/run_eval.py`：仍为 **126/126 全过**，保护网未被破坏。

2. **全量报告重生成**
   - 后台调用本机 API（`http://127.0.0.1:8000/api/report`，`dry_run:False`）重生成全部 51 个客户报告。
   - 结果：**51 成功 / 0 失败**，每个报告第 6 章「案例参考」已填充匹配案例。
   - 日志：`eval/regen.log`

3. **静态快照重导出**
   - 运行 `python3 static_export.py` 重导 `web/snapshot.json`（51 客户 / 51 份报告）。

4. **公网重新部署（静态只读大盘）**
   - 重新 CloudStudio 部署 `诊断台/web/` 目录。
   - 链接：https://349a08f059e14b86937b3346fa9a6d15.app.workbuddy.link

5. **GitHub Pages 同步更新**
   - 发现用户打开的是 `https://averymeng.github.io/ad-diagnosis-agent/`（gh-pages 分支），其 `snapshot.json` 仍为 8/20 旧版，导致第 6 章为空。
   - 已将最新 `web/` 内容（snapshot.json + 51 份报告 HTML）推送到 `gh-pages` 分支：
     - 提交 `52b1752`
   - GitHub Pages 缓存刷新通常需要几分钟。

### 备注
- 公网为静态只读快照：后续若再改案例/报告，需重跑 `static_export.py` 并同时更新 CloudStudio + gh-pages。
- 实时全功能（在线生成/审核）仅本机 `localhost:8000` 提供，需 `DEEPSEEK_API_KEY` 持续运行。
- 若打开后仍显示旧内容，请强制刷新（Mac `Cmd+Shift+R` / Win `Ctrl+F5`）或开无痕窗口。

---

## 2026-08-23 扩建「客户经营」（销售工作台子模块，前端只读分析层）

**操作人**：AIPM 项目协作
**时间**：2026-08-23 13:40–14:30 (GMT+8)

### 目标
面向平台商业化销售的客户经营看板，Phase 1+2：健康分 / 预警 / 分群卡 + 打法推荐 / 覆盖度自检。
**硬约束**：不破坏诊断引擎——工作台全部为前端只读计算，不修改任何数据库，不新增后端写入接口。

### 动作
1. **静态快照补充案例库只读导出**
   - `static_export.py` 新增 `cases` 字段（只读 `diag_case`，关联行业/赛道/打法/结果）。
   - 重导 `web/snapshot.json`：51 客户 / 51 报告 / **56 案例**。

2. **新增 `web/workbench.js`（纯前端，基于 SNAP 计算）**
   - 健康分公式（满分 100，可解释）：① 消耗动能（近 7 天 vs 前 7 天，下滑扣分上限 25）② 指标变化惩罚（9 核心指标，上限 35）③ 状态修正（需关注 −10 / 需行动 −22）④ 异常惩罚（上限 10）。分群 ≥80 健康 / 60–79 关注 / <60 预警。
   - 打法推荐：异常 location/reason → 规则化建议；并匹配**同赛道案例库**历史打法（复用 RAG 来源）。
   - 覆盖度自检：诊断覆盖 / 异常预警 / 案例覆盖 矩阵，标注缺口。
   - 分布实测：健康 29 / 关注 13 / 预警 9（合理区分度）。

3. **挂载到前端**
   - `web/index.html`：新增导航「🎯 客户经营」+ `workbenchView` 容器 + 引入 `workbench.js`。
   - `web/app.js`：`showView` 增加 `workbench` 分支（确保快照后渲染）。
   - `web/style.css`：补充工作台样式。

### 部署
- CloudStudio 重新部署 `web/`（链接不变）：https://349a08f059e14b86937b3346fa9a6d15.app.workbuddy.link
- gh-pages 推送最新 `web/`（提交 `be263e8`），GitHub Pages 同步。
- 本机 `localhost:8000` 直接读取磁盘文件，已在运行，强刷即可见。

### 遗留（待办，方案2 数据清理）
- `diag_case` 中 **AI培训 赛道 23 条案例的 `action_taken` 均为占位符**（挖矿脚本当时未回填），导致该赛道「打法推荐」只出规则化建议、无案例匹配。其他赛道（酒店住宿/休闲娱乐/婚纱摄影等）案例真实可用。如需全赛道打通，需清理这 23 条底层数据。

---

## 2026-08-23 客户经营模块改名 + 去冗余重构

**操作人**：AIPM 项目协作
**时间**：2026-08-23 14:35–15:10 (GMT+8)

### 背景
用户反馈：① 新增模块不该叫「销售工作台」（整个项目扩建后产品名才是销售工作台，子模块需另命名）；② 页面把 51 个客户名单全陈列，冗余、抓不住重点。

### 决策
- 子模块命名：**客户经营**（强调持续经营动作）；产品名「销售工作台」保留在 README/项目大标题。
- 页面重构：概览只给数字、全站只亮「预警」一个重点、其余详情按需展开。

### 动作
1. 全局改名：`index.html` 导航、`app.js` 标题、`workbench.js` 标题/注释、`style.css` 注释，模块名 销售工作台 → 客户经营。
2. 删除「客户健康分」全量 51 行名单表 → 改为「客户分群概览」3 卡（数量+均分+占比，不列名；点开才展开该群名单）。
3. 风险预警清单：仅列 `tier=预警` 客户（约 9 个，唯一重点区），点击展开打法；不再把有文案提示的客户都算进来。
4. 覆盖度自检：从「51 行逐客户表」改为「按赛道汇总表（约 13 行）+ 仅列缺口客户」。
5. 新增 CSS：分群卡 / 展开交互。

### 部署
- CloudStudio 重新部署 `web/`（链接不变）；gh-pages 推送（提交 `bae56f9`）；main 提交 `0a23334`。
- 本机 `localhost:8000` 强刷即见。

### 未做（按用户约定，稍后一起行动）
- **方案2 数据清理**：`diag_case` 中 AI培训 赛道 23 条占位符案例仍待清理（见上节遗留）。

---

## 2026-08-23 广告销售工作台 UI 调整 + AI培训占位案例清理

**操作人**：AIPM 项目协作
**时间**：2026-08-23 15:00–15:15 (GMT+8)

### 动作
1. **侧边栏重构**
   - 顶部产品名：「诊断台」→「广告销售工作台」。
   - 去掉「MAIN」「销售」分组标题。
   - 导航顺序改为：客户经营 → 当日监控 → 客户详情 → 手动录入。

2. **客户经营页面精简**
   - 去掉「销售工作台 · 客户经营看板（只读分析）」副标题。
   - 删除「风险预警」独立列表（截图中的 9 条冗余预警区）。
   - 保留 KPI 条、客户分群概览、打法推荐、覆盖度自检。

3. **方案2：AI培训 23 条占位符案例清理**
   - 将 `diag_case` 中 sector_id=1（AI培训）的 23 条案例全部标记 `referenceable=0`。
   - 其中 10 条 `action_taken=None`，13 条为「（待补：来自策略章节）」占位符。
   - 清理后该赛道不再被计入「案例覆盖赛道」，也不再出现在同赛道历史打法匹配中。
   - 重跑 `static_export.py` 更新 `web/snapshot.json`。

### 部署
- CloudStudio 重新部署 `web/`（链接不变）：https://349a08f059e14b86937b3346fa9a6d15.app.workbuddy.link
- gh-pages 推送最新 `web/`（提交 `82e9401`）。
- main 提交 `1935dcf`。
- 本机 `localhost:8000` 直接读取磁盘文件与数据库，强刷即见。

---

## 2026-08-21 项目名统一

- 将仓库内项目名统一为「客户广告投放诊断台」：README、PRD、AIPM 底稿标题已改。
- 提交 `1768498`（push 至 origin/main）。
